apiVersion: v1
kind: Service
metadata:
  name: webhook-stage-service # 🛠️
  namespace: instanode #🚀
spec:
  selector:
    app: webhook-stage # Your deployment name 💼
  ports:
    - protocol: TCP
      port: 80 #🌐
      targetPort: 4242 #🖥️
  type: ClusterIP #🌟
