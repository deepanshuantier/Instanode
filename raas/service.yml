apiVersion: v1
kind: Service
metadata:
  name: raas-stage-service # 🛠️
  namespace: instanode #🚀
spec:
  selector:
    app: raas-stage # Your deployment name 💼
  ports:
    - protocol: TCP
      port: 80 #🌐
      targetPort: 3007 #🖥️
  type: ClusterIP #🌟
