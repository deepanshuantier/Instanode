apiVersion: v1
kind: Service
metadata:
  name: admin-stage-service # 🛠️
  namespace: instanode #🚀
spec:
  selector:
    app: admin-stage # Your deployment name 💼
  ports:
    - protocol: TCP
      port: 80 #🌐
      targetPort: 3006 #🖥️
  type: ClusterIP #🌟
